
def test_pheno_execute(pheno):
    """Test actual execution of pheno."""
    # FIXME: Below works, but NM rc=107 because data not copied yet. That's final step to fix.
    # copy = pheno.copy()
    # copy.execute.estimate()
